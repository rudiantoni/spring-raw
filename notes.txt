Marcas
Toyota, Honda, Nissan, Volks

Modelos
Corolla, Civic, Sentra, Gol

Usar 2 gets para cada endpoint
@GetMapping("/marcas") // Retornar todas as marcas
@GetMapping("/marcas/{id}") // Retornar o objeto do id passado

@DeleteMapping("/marcas/{id}") // Deletar o objeto do id passado Sem retorno

@PostMapping("/marcas") // Incluir marca {id, descrição}

@PutMapping("/marcas/{id}") // Alterar objeto completo modelo {id, descrição, marca}

//@PatchMapping("/marcas/{id}/{atrib}") // Alterar atributo específico do objeto modelo {descrição | marca} 

@PostMapping("/marcas") // Incluir marca {id, descrição}

// Incluir modelo {id, descrição, marca}

